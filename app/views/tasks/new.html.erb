<%= simple_form_for @task, url: area_protocol_tasks_path, method: :post do |f| %>
<h2>Create Task: <%= link_to "Back", area_protocol_path(@proto, @area) %>
</h2>
<%= f.input :title, label: "Titre:" %>
<%= f.input :content, label: "Description:" %>
<button type="button" class="btn btn-outline-primary">
  <%= f.button :submit, value: 'Add', "data-disable-with" => "Creating.." %>
</button>
   <% end %>


